Noteutils
=========

A utility for validating note sections (`readelf -n`)

## Usage

```bash
$ noteutils validate --spec examples/spec.yml /home/nick/code/src/github.com/skpr/compass/tracing/compass.so
```
